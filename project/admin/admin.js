(() => {
  const translations = {
    "NovaPay Control": "Bảng điều khiển NovaPay", "Dashboard": "Tổng quan", "Website Settings": "Cài đặt website", "Branding": "Thương hiệu", "CTA & Links": "CTA & liên kết", "Hero": "Hero", "Images": "Hình ảnh", "Social Links": "Mạng xã hội", "Theme": "Giao diện", "Sections": "Các phần hiển thị", "FAQ": "Câu hỏi thường gặp", "Activity Log": "Nhật ký hoạt động", "Preview": "Xem trước", "Logout": "Đăng xuất", "Admin Login": "Đăng nhập quản trị", "Admin Login | Admin": "Đăng nhập quản trị | Quản trị", "Email or username": "Email hoặc tên đăng nhập", "Password": "Mật khẩu", "Show": "Hiện", "Sign in": "Đăng nhập", "Signing in...": "Đang đăng nhập...", "Dashboard overview": "Tổng quan hệ thống", "Website status": "Trạng thái website", "Website name": "Tên website", "Telegram": "Telegram", "Live Chat": "Live Chat", "Connected": "Đã kết nối", "Not set": "Chưa thiết lập", "Configuration workflow": "Quy trình cấu hình", "Save Draft": "Lưu bản nháp", "Publish Changes": "Xuất bản thay đổi", "Reset Draft": "Khôi phục bản nháp", "Open Website": "Mở website", "Preview Website": "Xem trước website", "Last checked:": "Kiểm tra lần cuối:", "Edit Website": "Sửa website", "Edit Links": "Sửa liên kết", "Edit Branding": "Sửa thương hiệu", "Website settings": "Cài đặt website", "Website Name": "Tên website", "Browser Title": "Tiêu đề trình duyệt", "Website Description": "Mô tả website", "Meta Description": "Mô tả SEO", "Contact Email": "Email liên hệ", "Support Email": "Email hỗ trợ", "Branding": "Thương hiệu", "Logo Mark": "Ký hiệu logo", "Logo Text": "Tên logo", "Logo URL": "Đường dẫn logo", "Favicon URL": "Đường dẫn favicon", "Links": "Liên kết", "CTA and links": "CTA và liên kết", "Telegram URL": "Đường dẫn Telegram", "Live Chat URL": "Đường dẫn Live Chat", "Support URL": "Đường dẫn hỗ trợ", "Facebook URL": "Đường dẫn Facebook", "Instagram URL": "Đường dẫn Instagram", "YouTube URL": "Đường dẫn YouTube", "LinkedIn URL": "Đường dẫn LinkedIn", "X URL": "Đường dẫn X", "Hero content": "Nội dung Hero", "Hero Badge": "Nhãn Hero", "Hero Title": "Tiêu đề Hero", "Hero Subtitle": "Mô tả Hero", "Primary Button Text": "Nội dung nút chính", "Primary Button Link": "Liên kết nút chính", "Secondary Button Text": "Nội dung nút phụ", "Secondary Button Link": "Liên kết nút phụ", "Hero Image URL": "Đường dẫn hình Hero", "Hero Background URL": "Đường dẫn nền Hero", "Theme colors": "Màu giao diện", "Primary Color": "Màu chính", "Secondary Color": "Màu phụ", "Accent Color": "Màu nhấn", "Background Color": "Màu nền", "Text Color": "Màu chữ", "Button Color": "Màu nút", "Section visibility": "Hiển thị các phần", "FAQ management": "Quản lý FAQ", "Image management": "Quản lý hình ảnh", "Activity log": "Nhật ký hoạt động", "Date": "Ngày", "Action": "Thao tác", "Admin": "Quản trị viên", "Status": "Trạng thái", "Upload image": "Tải hình ảnh", "Slot": "Vị trí", "File": "Tệp hình ảnh", "Upload to Draft": "Tải vào bản nháp", "Save Draft": "Lưu bản nháp", "Draft saved successfully": "Đã lưu bản nháp", "Published successfully": "Đã xuất bản thành công", "Draft reset to published": "Đã khôi phục bản nháp theo bản đã xuất bản", "Request failed": "Yêu cầu thất bại", "Authentication required": "Cần đăng nhập quản trị"
  };
  function translateAdmin() { document.documentElement.lang = "vi"; const walker = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT); const nodes = []; while (walker.nextNode()) nodes.push(walker.currentNode); nodes.forEach((node) => { const text = node.nodeValue.trim(); if (translations[text]) node.nodeValue = node.nodeValue.replace(text, translations[text]); }); document.querySelectorAll("input[placeholder], textarea[placeholder]").forEach((el) => { if (translations[el.placeholder]) el.placeholder = translations[el.placeholder]; }); }
  translateAdmin();
  const renameLegacyBrand = () => { const walker = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT); const nodes = []; while (walker.nextNode()) nodes.push(walker.currentNode); nodes.forEach((node) => { node.nodeValue = node.nodeValue.replaceAll("NovaPay", "IndreamPay").replaceAll("FINORA", "IndreamPay"); }); document.querySelectorAll(".admin-mark").forEach((mark) => { mark.textContent = "iD"; mark.style.background = "linear-gradient(135deg,#ffb52e,#f47716)"; }); document.querySelectorAll("input[placeholder]").forEach((input) => { input.placeholder = input.placeholder.replaceAll("NovaPay", "IndreamPay").replaceAll("FINORA", "IndreamPay"); }); }; renameLegacyBrand();
  const extraTranslations = { "Experience & Effects": "Hiệu ứng & chuyển động", "Motion controls": "Điều khiển chuyển động", "Control the public website animation and scrolling behavior without editing code.": "Điều khiển hiệu ứng và chuyển động của website mà không cần sửa mã.", "Enable animations": "Bật hiệu ứng chuyển động", "Enable scrolling marquees": "Bật nội dung chạy ngang/dọc", "Enable 3D / parallax effects": "Bật hiệu ứng 3D / parallax", "Marquee speed": "Tốc độ nội dung chuyển động", Slow: "Chậm", Normal: "Bình thường", Fast: "Nhanh", "Very fast": "Rất nhanh", "Live preview": "Xem trước trực tiếp", "Money, made clearer.": "Quản lý tiền rõ ràng hơn.", "See how your palette looks across the public website.": "Xem bảng màu hiển thị trên website công khai.", "Restore published": "Khôi phục bản đã xuất bản", "Undo changes": "Hoàn tác thay đổi", "Open account": "Mở tài khoản", "Restore website": "Khôi phục website", "Delete log": "Xóa nhật ký", Controls: "Thao tác", "Activity log deleted": "Đã xóa nhật ký", "Website restored": "Đã khôi phục website", "Activity entry not found": "Không tìm thấy bản ghi nhật ký", "This activity entry has no website snapshot": "Bản ghi này không có snapshot website", "No activity yet": "Chưa có hoạt động nào", "Delete this activity log entry?": "Xóa bản ghi nhật ký này?", "Restore the website to this published snapshot?": "Khôi phục website về snapshot đã xuất bản này?", Published: "Đã xuất bản", Connected: "Đã kết nối", "Save changes as draft, then publish when the public website is ready.": "Lưu thay đổi thành bản nháp, sau đó xuất bản khi website đã sẵn sàng.", "No activity yet": "Chưa có hoạt động nào" };
  const translateExtras = () => { const extraWalker = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT); const extraNodes = []; while (extraWalker.nextNode()) extraNodes.push(extraWalker.currentNode); extraNodes.forEach((node) => { const text = node.nodeValue.trim(); if (extraTranslations[text]) node.nodeValue = node.nodeValue.replace(text, extraTranslations[text]); }); }; translateExtras(); new MutationObserver(translateExtras).observe(document.body, { childList: true, subtree: true });
  const page = document.body.dataset.page;
  if (page === "socials") { window.location.replace("links.html"); return; }
  if (page === "theme") { window.location.replace("settings.html"); return; }
  if (page === "theme") document.querySelector("#themePreview")?.remove();
  if (page === "images") { const script = document.createElement("script"); script.src = "images-manager.js?v=2"; document.body.appendChild(script); }
  const $ = (selector) => document.querySelector(selector);
  const toast = (message, error = false) => { const el = $("#toast"); if (!el) return; el.textContent = message; el.style.background = error ? "#a33b3b" : "#13243b"; el.classList.add("show"); setTimeout(() => el.classList.remove("show"), 2600); };
  const pendingRequests = new Map();
  async function api(url, options = {}) { const method = options.method || "GET"; const requestKey = `${method}:${url}`; if (pendingRequests.has(requestKey)) return pendingRequests.get(requestKey); const request = fetch(url, { credentials: "same-origin", headers: { "Content-Type": "application/json", ...(options.headers || {}) }, ...options }).then(async (response) => { const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "Request failed"); if (url === "/api/admin/publish") localStorage.setItem("websiteConfigUpdated", String(Date.now())); return body; }).finally(() => pendingRequests.delete(requestKey)); pendingRequests.set(requestKey, request); return request; }
  document.addEventListener("click", (event) => { const button = event.target.closest?.("button"); if (!button || button.disabled || !/publish|cập nhật|xuất bản|đăng thay đổi/i.test(button.textContent)) return; button.disabled = true; button.dataset.busy = "true"; window.setTimeout(() => { button.disabled = false; button.dataset.busy = "false"; }, 4000); }, true);
  function nav() { const toggle = $("#menuToggle"); const sidebar = $(".admin-sidebar"); const links = $(".admin-nav"); links?.querySelectorAll('[href="socials.html"],[href="theme.html"]').forEach((link) => link.remove()); if (links && !links.querySelector('[href="experience.html"]')) { const link = document.createElement("a"); link.href = "experience.html"; link.textContent = "Experience & Effects"; links.insertBefore(link, links.querySelector('[href="sections.html"]') || null); } toggle?.addEventListener("click", () => sidebar.classList.toggle("open")); $("#logout")?.addEventListener("click", async () => { await api("/api/admin/logout", { method: "POST" }); location.href = "/admin/login.html"; }); document.querySelectorAll(".admin-nav a").forEach((link) => { if (link.dataset.page === page) link.classList.add("active"); }); }
  function shell(title) { document.title = `${translations[title] || title} | Quản trị`; nav(); }
  const field = (name) => document.querySelector(`[name="${name}"]`);
  function fill(data) { Object.entries(data || {}).forEach(([key, value]) => { const el = field(key); if (!el) return; if (el.type === "checkbox") el.checked = Boolean(value); else el.value = value ?? ""; }); }
  function collect(container = document) { const data = {}; container.querySelectorAll("[name]").forEach((el) => { data[el.name] = el.type === "checkbox" ? el.checked : el.value; }); return data; }
  async function protectedPage() { try { const me = await api("/api/admin/me"); const user = $("#adminUser"); if (user) user.textContent = me.admin.email; return true; } catch { location.href = "/admin/login.html"; return false; } }
  async function configPage(key, title) { if (!await protectedPage()) return; shell(title); try { const result = await api(`/api/admin/${key}`); if (key === "faq") { const faqField = field("faq"); if (faqField) faqField.value = JSON.stringify(result.data || [], null, 2); } else { fill(result.data); } const form = $("#configForm"); const saveDraft = async () => { const payload = collect(); if (key === "faq") payload.faq = JSON.parse(payload.faq); await api(`/api/admin/${key}`, { method: "PUT", body: JSON.stringify(payload) }); }; form?.addEventListener("submit", async (event) => { event.preventDefault(); try { await saveDraft(); toast("Đã lưu bản nháp"); } catch (error) { toast(error.message, true); } }); if (form && !$("#savePublish")) { const publishButton = document.createElement("button"); publishButton.id = "savePublish"; publishButton.type = "button"; publishButton.className = "admin-btn"; publishButton.textContent = "Lưu và cập nhật website"; form.querySelector(".admin-actions")?.appendChild(publishButton); publishButton.addEventListener("click", async () => { if (!window.confirm("Xác nhận đưa link và nội dung này lên website?")) return; try { await saveDraft(); await api("/api/admin/publish", { method: "POST" }); toast("Đã lưu và cập nhật website thành công"); } catch (error) { toast(error.message, true); } }); } } catch (error) { toast(error.message, true); } }
  async function login() { const emailField = field("email"); if (emailField) emailField.type = "text"; $("#togglePassword")?.addEventListener("click", () => { const input = field("password"); input.type = input.type === "password" ? "text" : "password"; }); $("#loginForm")?.addEventListener("submit", async (event) => { event.preventDefault(); const button = $("#loginButton"); const error = $("#loginError"); button.disabled = true; button.textContent = "Đang đăng nhập..."; error.textContent = ""; try { await api("/api/admin/login", { method: "POST", body: JSON.stringify(collect()) }); location.href = "/admin/dashboard.html"; } catch (err) { error.textContent = "Thông tin đăng nhập không chính xác"; button.disabled = false; button.textContent = "Đăng nhập"; } }); }
  async function dashboard() { if (!await protectedPage()) return; shell("Dashboard"); const result = await api("/api/admin/config"); const draft = result.draft; $("#websiteName").textContent = draft.settings.websiteName; $("#telegramStatus").textContent = draft.links.telegramUrl ? "Connected" : "Not set"; $("#chatStatus").textContent = draft.links.liveChatUrl ? "Connected" : "Not set"; $("#lastUpdated").textContent = new Date().toLocaleString(); const publish = async () => { if (!window.confirm("Xác nhận cập nhật nội dung này lên website công khai?")) return; try { await api("/api/admin/publish", { method: "POST" }); toast("Đã xác nhận và cập nhật website"); } catch (error) { toast(error.message, true); } }; if (!$("#confirmPublish") && $("#publish")) { const confirmButton = document.createElement("button"); confirmButton.id = "confirmPublish"; confirmButton.className = "admin-btn"; confirmButton.textContent = "Xác nhận cập nhật website"; $("#publish").parentElement.appendChild(confirmButton); } $("#publish")?.addEventListener("click", publish); $("#confirmPublish")?.addEventListener("click", publish); $("#reset")?.addEventListener("click", async () => { try { await api("/api/admin/reset", { method: "POST" }); toast("Đã khôi phục bản nháp"); } catch (error) { toast(error.message, true); } }); }
  async function activity() { if (!await protectedPage()) return; shell("Activity"); const result = await api("/api/admin/activity"); const body = $("#activityRows"); body.innerHTML = result.data.map((entry) => `<tr><td>${new Date(entry.date).toLocaleString()}</td><td>${entry.action}</td><td>${entry.admin}</td><td><span class="status">${entry.status}</span></td><td class="activity-actions">${entry.snapshot ? `<button class="admin-btn" data-restore="${entry.id}" type="button">Restore website</button>` : ""}<button class="admin-btn danger" data-delete="${entry.id}" type="button">Delete log</button></td></tr>`).join("") || "<tr><td colspan=5>No activity yet</td></tr>"; body.querySelectorAll("[data-delete]").forEach((button) => button.addEventListener("click", async () => { if (!confirm("Delete this activity log entry?")) return; try { await api(`/api/admin/activity/${button.dataset.delete}`, { method: "DELETE" }); toast("Activity log deleted"); await activity(); } catch (error) { toast(error.message, true); } })); body.querySelectorAll("[data-restore]").forEach((button) => button.addEventListener("click", async () => { if (!confirm("Restore the website to this published snapshot?")) return; try { await api(`/api/admin/activity/${button.dataset.restore}/restore`, { method: "POST" }); toast("Website restored"); await activity(); } catch (error) { toast(error.message, true); } })); }
  async function sections() { await configPage("sections", "Sections"); }
  if (page === "login") login(); else if (page === "dashboard") dashboard(); else if (page === "activity") activity(); else if (page === "images") protectedPage().then((allowed) => { if (allowed) shell("Images"); }); else if (page === "sections") sections(); else if (page === "settings") configPage("settings", "Website Settings"); else if (page === "branding") configPage("branding", "Branding"); else if (page === "links") configPage("links", "CTA & Links"); else if (page === "hero") configPage("hero", "Hero"); else if (page === "experience") configPage("experience", "Experience & Effects"); else if (page === "theme") configPage("theme", "Theme"); else if (page === "socials") configPage("socials", "Social Links"); else if (page === "faq") configPage("faq", "FAQ");
  async function setupThemePreview() { const form = $("#configForm"); if (!form || form.dataset.previewReady === "true") return; const inputs = form.querySelectorAll('input[type="color"]'); if (!inputs.length || !inputs[0].value) { window.setTimeout(setupThemePreview, 250); return; } const config = await api("/api/admin/config"); const original = {}; inputs.forEach((input) => { original[input.name] = input.value; }); const preview = document.createElement("div"); preview.className = "theme-preview-panel"; preview.id = "themePreview"; preview.innerHTML = "<div class=theme-preview-copy><span>Live preview</span><strong>Money, made clearer.</strong><p>See how your palette looks across the public website.</p><button type=button class=admin-btn data-preview-action=restore>Restore published</button><button type=button class=admin-btn data-preview-action=revert>Undo changes</button></div><div class=theme-preview-card><span class=preview-mark>F</span><div><strong>FINORA</strong><small>Secure everyday payments</small></div><button type=button class=preview-button>Open account</button></div>"; form.parentElement.insertBefore(preview, form); const render = () => { const values = Object.fromEntries([...inputs].map((input) => [input.name, input.value])); preview.style.setProperty("--preview-primary", values.primary); preview.style.setProperty("--preview-secondary", values.secondary); preview.style.setProperty("--preview-accent", values.accent); preview.style.setProperty("--preview-background", values.background); preview.style.setProperty("--preview-text", values.text); preview.style.setProperty("--preview-button", values.button); inputs.forEach((input) => { const code = input.parentElement.querySelector(".color-code"); if (code) code.textContent = input.value.toUpperCase(); }); }; inputs.forEach((input) => input.addEventListener("input", render)); preview.querySelector('[data-preview-action="revert"]').addEventListener("click", () => { inputs.forEach((input) => { input.value = original[input.name]; }); render(); }); preview.querySelector('[data-preview-action="restore"]').addEventListener("click", async () => { const published = config.published.theme || {}; inputs.forEach((input) => { if (published[input.name]) input.value = published[input.name]; }); render(); await api("/api/admin/theme", { method: "PUT", body: JSON.stringify(published) }); toast("Đã khôi phục màu đã xuất bản"); }); form.dataset.previewReady = "true"; render(); }
  openInlineDraftPreview = ((original) => (section = "top", anchor) => { original(section, anchor); const frame = document.querySelector("#inlineDraftPreview iframe"); if (frame) frame.src = `/?preview=draft&previewId=${Date.now()}#${section}`; frame?.addEventListener("load", () => { try { frame.contentDocument?.getElementById(section)?.scrollIntoView({ block: "start" }); } catch {} }, { once: true }); })(openInlineDraftPreview);
  async function setupHeroLinkFields(heroData) { if (page !== "hero") return; const form = document.querySelector("#configForm"); if (!form) return; const config = await api("/api/admin/config"); const links = config.draft.links || {}; const channels = [{ key: "telegram", label: "Telegram", url: links.telegramUrl }, { key: "live-chat", label: "Live Chat", url: links.liveChatUrl }, { key: "support", label: "Support", url: links.supportUrl }, { key: "facebook", label: "Facebook", url: links.facebookUrl }, { key: "instagram", label: "Instagram", url: links.instagramUrl }, { key: "youtube", label: "YouTube", url: links.youtubeUrl }, { key: "linkedin", label: "LinkedIn", url: links.linkedinUrl }, { key: "x", label: "X", url: links.xUrl }].filter((item) => item.url && item.url !== "#" && !/(your[-_]|YOUR_|example\.com|placeholder)/i.test(item.url)); ["primaryButtonLink", "secondaryButtonLink"].forEach((name) => { const input = form.querySelector(`[name="${name}"]`); if (!input || input.tagName === "SELECT") return; const select = document.createElement("select"); select.name = name; select.innerHTML = `<option value="">Chọn kênh đã liên kết</option>${channels.map((item) => `<option value="${item.key}">${item.label}</option>`).join("")}`; select.value = input.value; input.replaceWith(select); }); }
  if (page === "hero") window.setTimeout(() => { ["imageUrl", "backgroundUrl"].forEach((name) => document.querySelector(`#configForm [name="${name}"]`)?.closest(".form-field")?.remove()); setupHeroLinkFields(); }, 500);
  if (page === "experience") window.setTimeout(() => { const grid = document.querySelector("#configForm .section-switches"); if (!grid) return; const controls = [["revealEnabled", "Hiệu ứng xuất hiện khi cuộn"], ["liveDataEnabled", "Chuyển động dữ liệu và biểu đồ"]]; controls.forEach(([name, label]) => { if (grid.querySelector(`[name="${name}"]`)) return; const row = document.createElement("div"); row.className = "switch-row"; row.innerHTML = `<span>${label}</span><input class="toggle" name="${name}" type="checkbox">`; grid.appendChild(row); }); }, 50);
  window.openInlineDraftPreview = openInlineDraftPreview;
  document.addEventListener("click", (event) => { const button = event.target.closest?.("[data-draft-preview]"); if (!button) return; const form = document.querySelector("#configForm"); if (!form) return; const key = page; const data = collect(form); if (key === "faq") { try { data.faq = JSON.parse(data.faq); } catch {} } sessionStorage.setItem("adminPreviewOverride", JSON.stringify({ key, data, time: Date.now() })); }, true);
  document.addEventListener("submit", (event) => { if (event.target.id !== "configForm") return; const sections = { settings: "top", branding: "top", links: "support", hero: "top", experience: "top", theme: "top", sections: "features", socials: "support", faq: "faq" }; window.setTimeout(() => { const form = event.target; const data = collect(form); if (page === "faq") { try { data.faq = JSON.parse(data.faq); } catch {} } sessionStorage.setItem("adminPreviewOverride", JSON.stringify({ key: page, data, time: Date.now() })); openInlineDraftPreview(sections[page] || "top"); }, 500); });
  if (page === "theme") window.setTimeout(setupThemePreview, 50);
  if (page === "settings") window.setTimeout(setupNavEditor, 50);
  if (!["login", "dashboard", "activity", "images", "preview"].includes(page)) { const restoreTimer = window.setInterval(() => { const form = document.querySelector("#configForm"); const actions = form?.querySelector(".admin-actions"); if (!form || !actions || form.dataset.initialSnapshot === "true") return; const initial = collect(form); if (!Object.values(initial).some(Boolean)) return; form.dataset.initialSnapshot = "true"; if (actions.querySelector("[data-restore-initial]")) return; const restore = document.createElement("button"); restore.type = "button"; restore.className = "admin-btn secondary"; restore.dataset.restoreInitial = "true"; restore.textContent = "Khôi phục bản đầu"; restore.addEventListener("click", () => { fill(initial); form.querySelectorAll('input[type="color"]').forEach((input) => input.dispatchEvent(new Event("input", { bubbles: true }))); toast("Đã khôi phục nội dung ban đầu"); }); actions.appendChild(restore); window.clearInterval(restoreTimer); }, 300); }
  if (!["login", "dashboard", "activity", "images", "preview"].includes(page)) window.setTimeout(() => { const form = document.querySelector("#configForm"); const actions = form?.querySelector(".admin-actions"); if (!form || !actions || actions.querySelector("[data-restore-initial]")) return; const initial = collect(form); const restore = document.createElement("button"); restore.type = "button"; restore.className = "admin-btn secondary"; restore.dataset.restoreInitial = "true"; restore.textContent = "Khôi phục bản đầu"; restore.addEventListener("click", () => { fill(initial); form.querySelectorAll('input[type="color"]').forEach((input) => input.dispatchEvent(new Event("input", { bubbles: true }))); toast("Đã khôi phục nội dung ban đầu"); }); actions.appendChild(restore); }, 50);
  function openInlineDraftPreview(section = "top", anchor = document.querySelector(".admin-content")) { let panel = document.querySelector("#inlineDraftPreview"); if (!panel) { panel = document.createElement("div"); panel.id = "inlineDraftPreview"; panel.className = "admin-card inline-draft-preview"; panel.style.marginTop = "18px"; panel.innerHTML = "<div style=\"display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:12px\"><div><h2>Xem trước bản nháp</h2><p class=stat-label>Đây là bản nháp hiện tại, chưa được xuất bản.</p></div><button type=button class=admin-btn secondary data-close-preview>Đóng xem trước</button></div><iframe title=\"Xem trước website draft\" style=\"width:100%;height:620px;border:1px solid var(--admin-line);border-radius:12px;background:#0b102e\"></iframe>"; anchor?.appendChild(panel); panel.querySelector("[data-close-preview]").addEventListener("click", () => panel.hidden = true); } panel.hidden = false; panel.querySelector("iframe").src = `/?preview=draft#${section}`; panel.scrollIntoView({ behavior: "smooth", block: "start" }); }
  if (!["login", "dashboard", "activity", "images", "preview"].includes(page)) window.setTimeout(() => { const actions = document.querySelector("#configForm .admin-actions"); if (!actions || actions.querySelector("[data-draft-preview]")) return; const sections = { settings: "top", branding: "top", links: "support", hero: "top", experience: "top", theme: "top", sections: "features", socials: "support", faq: "faq" }; const preview = document.createElement("button"); preview.className = "admin-btn secondary"; preview.type = "button"; preview.dataset.draftPreview = "true"; preview.textContent = "Xem trước bản nháp"; preview.addEventListener("click", () => openInlineDraftPreview(sections[page] || "top")); actions.appendChild(preview); }, 50);
  async function setupNavEditor() { const content = document.querySelector(".admin-content"); if (!content || document.querySelector("#navEditor")) return; const result = await api("/api/admin/nav"); const panel = document.createElement("div"); panel.id = "navEditor"; panel.className = "admin-card"; panel.style.marginTop = "18px"; panel.innerHTML = `<h2>Menu website</h2><p class="stat-label">Bật Hiển thị để giữ mục menu. Bỏ tích sẽ ẩn mục đó ngay sau khi lưu. Nếu xóa liên kết, hệ thống tự dùng section mặc định.</p><div class="nav-editor-list"></div><div class="admin-actions"><button type="button" class="admin-btn" data-save-nav>Lưu và cập nhật menu</button></div>`; content.appendChild(panel); const list = panel.querySelector(".nav-editor-list"); const nav = result.data || []; const defaults = ["#features", "#wallet", "#rewards", "#security", "#support"]; list.innerHTML = nav.map((item, index) => `<div class="form-grid" style="margin:12px 0"><div class="form-field"><label>Tên mục ${index + 1}</label><input data-nav-label value="${String(item.label || "").replace(/"/g, "&quot;")}"></div><div class="form-field"><label>Liên kết (#section hoặc URL)</label><input data-nav-url value="${String(item.url || "").replace(/"/g, "&quot;")}" placeholder="Để trống = ${defaults[index] || "#top"}"></div><div class="form-field"><label>Hiển thị</label><input data-nav-enabled type="checkbox" ${item.enabled !== false ? "checked" : ""}></div></div>`).join(""); panel.querySelector("[data-save-nav]").addEventListener("click", async (event) => { const button = event.currentTarget; button.disabled = true; const updated = [...list.querySelectorAll(".form-grid")].map((row, index) => ({ label: row.querySelector("[data-nav-label]").value, url: row.querySelector("[data-nav-url]").value || defaults[index] || "#top", enabled: row.querySelector("[data-nav-enabled]").checked })); try { await api("/api/admin/nav", { method: "PUT", body: JSON.stringify(updated) }); await api("/api/admin/publish", { method: "POST" }); localStorage.setItem("websiteConfigUpdated", String(Date.now())); toast("Đã cập nhật menu lên website"); } catch (error) { toast(error.message, true); } finally { button.disabled = false; } }); }
})();
